<Project Sdk="Microsoft.NET.Sdk">
  <!--
    Unit tests. These must never touch the live registry - every test runs
    against an in-memory fake so the suite is deterministic on any machine.
    Live-machine coverage belongs in AddRem.IntegrationTests.
  -->

  <ItemGroup>
    <ProjectReference Include="..\..\src\AddRem\AddRem.csproj" />
  </ItemGroup>
</Project>
