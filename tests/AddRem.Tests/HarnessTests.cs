namespace AddRem.Tests;

/// <summary>
/// Proves the unit-test harness builds, discovers and runs. Real suites arrive
/// with the registry abstraction in M2.
/// </summary>
public class HarnessTests
{
    [Fact]
    public void TestHarness_Runs()
    {
        Assert.True(true);
    }
}
